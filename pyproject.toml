[tool.poetry]
name = "dummy"                    # ← Фиктивное имя для обхода проверки
version = "0.1.0"
package-mode = false

[tool.poetry.dependencies]
python = "^3.14"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"